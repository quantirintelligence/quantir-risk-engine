# Risk Model Artifacts

This public grant repository keeps the Rust model code but omits private runtime artifacts:

- trained model weights
- internal dataset artifacts
- other generated training outputs

That keeps the public snapshot focused on the scoring implementation and feature contract without publishing internal data assets.
